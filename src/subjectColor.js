// Subject badges are kept monochrome (a single neutral chip) for a clean, mostly
// colourless look. The signature stays the same so callers don't change.
export function subjectColor() {
  return '#3a4556'
}
